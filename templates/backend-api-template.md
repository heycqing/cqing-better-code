# backend-api.md — 接口契约登记（[项目名称]）

> **本文件是接口的单一真相来源。**
> 所有已交付的真实接口在此登记；后端缺口见 `TODO-backend.md`。
>
> **维护规则（P1）**：任何接口新增/修改，必须在同一操作内更新本文件。
> 代码接入了但本文件没有对应条目 = 变更未完成。
>
> 核验命令（会话收尾必跑）：
> ```bash
> grep -r "url:" src/ --include="*.ts" | grep -oP "'[^']*'" | sort -u
> ```
> 逐一在本文件查找。找不到 = 需要补登记。

---

## 公共规则

- 请求方式：[填写，如 POST / GET]
- Base URL：[填写，如 `/api/v1/`]
- 鉴权方式：[填写，如 token in header / query]
- 响应格式：[填写，如 `{ code, message, data }`]

---

## 接口清单

| # | 接口名 | URI | 对应功能 | 状态 |
|---|--------|-----|----------|------|
| 1 | [接口名] | [URI] | [对应页面/功能] | ✅ 已交付 |

---

## 接口详情

### 1. [接口名]

**URI**: `[METHOD] [full URI]`
**说明**: [一句话描述用途]

**入参**：

| 参数 | 说明 | 必填 | 类型 |
|------|------|------|------|
| [param] | [说明] | 是/否 | [类型] |

**出参**：`[结构描述]`

| 字段 | 说明 | 类型 |
|------|------|------|
| [field] | [说明] | [类型] |

**注意事项**：
- ⚠️ [填写字段映射陷阱、类型不一致、特殊行为等]

**真实报文**：
```json
[粘贴真实的请求/响应报文]
```

---

## 变更记录

| 日期 | 接口 | 变更内容 |
|------|------|----------|
| [日期] | [接口名] | [入参新增 xxx 字段] |
